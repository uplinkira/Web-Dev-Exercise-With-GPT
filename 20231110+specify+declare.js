// Specifies a rule for calculating the area of a rectangle
// 指定了一个计算矩形面积的规则
function calculateRectangleArea(length, width) {
    return length * width;
}

// Specifies a CSS style rule for changing the text color
// 指定了一个改变文本颜色的CSS样式规则
p {
    color: blue;
}

// Specifies behavior for handling user clicks
// 指定了处理用户点击的行为
document.getElementById('myButton').addEventListener('click', function() {
    // Code to be executed when the button is clicked
    // 当按钮被点击时要执行的代码
});

// Declares a variable named 'name' with a string value
// 声明一个名为 'name' 的变量，并赋予它一个字符串值
var name = "John";

// Declares a constant variable for the value of pi
// 声明一个常量变量来存储圆周率的值
const pi = 3.14159265359;

// Declares a function named 'calculateCircleArea' to calculate the area of a circle
// 声明一个名为 'calculateCircleArea' 的函数来计算圆的面积
function calculateCircleArea(radius) {
    return pi * radius * radius;
}

// Declares an array to store a list of names
// 声明一个数组来存储一组名字
var names = ["Alice", "Bob", "Charlie"];
